apiVersion: apps/v1
kind: Deployment
metadata:
  name: trem-giz-api
spec:
  selector:
    matchLabels:
      app: trem-giz-api
  template:
    metadata:
      labels:
        app: trem-giz-api
    spec:
      containers:
      - name: trem-giz-api
        image: trem-giz-api
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
          requests:
            memory: "256Mi"
            cpu: "250m"
        ports:
        - containerPort: 3000