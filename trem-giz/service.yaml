apiVersion: v1
kind: Service
metadata:
  name: trem-giz-api
  labels:
    app: trem-giz-api
spec:
  selector:
    app: trem-giz-api
  ports:
    - port: 3000
      targetPort: 5000
  type: LoadBalancer
